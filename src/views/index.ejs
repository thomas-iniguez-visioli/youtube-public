<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>YouTube Downloader - Ma Bibliothèque</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">

    <script src="https://cdn.lr-ingest.com/LogRocket.min.js" crossorigin="anonymous"></script>
    <script>window.LogRocket && window.LogRocket.init('kw8sds/youtube-zozss');</script>
    <script src="renderer.js"></script>

    <style>
        body {
            background-color: #0f0f0f;
            color: #ffffff;
            font-family: 'Roboto', sans-serif;
        }
        .navbar {
            background-color: #0f0f0f !important;
            border-bottom: 1px solid #272727;
        }
        .video-card {
            background: transparent;
            border: none;
            margin-bottom: 2rem;
            cursor: pointer;
            text-decoration: none;
            color: inherit;
        }
        .video-card:hover .video-thumbnail {
            border-radius: 0;
        }
        .video-thumbnail {
            width: 100%;
            aspect-ratio: 16/9;
            object-fit: cover;
            border-radius: 12px;
            transition: border-radius 0.2s;
        }
        .video-info {
            display: flex;
            margin-top: 12px;
        }
        .video-details {
            margin-left: 12px;
        }
        .video-title {
            font-size: 1rem;
            font-weight: 500;
            line-height: 1.4rem;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
            margin-bottom: 4px;
        }
        .video-meta {
            font-size: 0.85rem;
            color: #aaaaaa;
        }
        .tag-badge {
            background-color: #272727;
            color: #ffffff;
            font-size: 0.75rem;
            padding: 2px 8px;
            border-radius: 4px;
            margin-right: 4px;
            cursor: pointer;
        }
        .tag-badge:hover {
            background-color: #3f3f3f;
        }
        .search-container {
            max-width: 600px;
        }
        .form-control {
            background-color: #121212;
            border: 1px solid #333;
            color: white;
        }
        .form-control:focus {
            background-color: #121212;
            border-color: #1a73e8;
            color: white;
            box-shadow: none;
        }
        .tag-filter-section {
            padding: 15px 0;
            border-bottom: 1px solid #272727;
            margin-bottom: 20px;
            display: flex;
            overflow-x: auto;
            white-space: nowrap;
            gap: 10px;
        }
        .tag-filter-section::-webkit-scrollbar {
            height: 4px;
        }
        .tag-filter-section::-webkit-scrollbar-thumb {
            background: #333;
            border-radius: 10px;
        }
        .filter-chip {
            background-color: #272727;
            color: white;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.9rem;
            cursor: pointer;
            border: 1px solid transparent;
            transition: all 0.2s;
        }
        .filter-chip.active {
            background-color: white;
            color: black;
        }
        .filter-chip:hover:not(.active) {
            background-color: #3f3f3f;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark sticky-top">
        <div class="container-fluid">
            <a class="navbar-brand d-flex align-items-center" href="/">
                <img src="https://www.youtube.com/s/desktop/2823798a/img/favicon_32x32.png" width="30" height="30" class="me-2" alt="">
                <strong>YouTube Downloader</strong>
            </a>
            
            <div class="d-flex flex-grow-1 justify-content-center mx-4">
                <form id="commandForm" class="d-flex w-100 search-container m-0">
                    <input class="form-control me-2 rounded-pill ps-4" type="text" id="commandInput" placeholder="Coller une URL YouTube..." required>
                    <button class="btn btn-outline-light rounded-pill px-4" type="submit">Télécharger</button>
                </form>
            </div>
            <div class="d-flex">
                <a href="/history" class="btn btn-outline-info rounded-pill px-4">Historique</a>
            </div>
        </div>
    </nav>

    <div class="container-fluid px-4">
        <% if (typeof channel !== 'undefined' && channel) { %>
            <div class="d-flex align-items-center mb-4 mt-3">
                <h2 class="text-white">Vidéos de : <%= channel %></h2>
                <a href="/" class="btn btn-outline-secondary btn-sm ms-3">Retour à l'accueil</a>
            </div>
        <% } %>
        <!-- Section Filtres Inline -->
        <div class="tag-filter-section">
            <div class="filter-chip active" onclick="filterByTag('all', this)">Tous</div>
            <% const uniqueTags = [... new Set(results.reduce((acc, result) => acc.concat(result.tags), []))]; %>
            <% uniqueTags.forEach(function(tag){ %>
                <div class="filter-chip" onclick="filterByTag('<%= tag %>', this)"><%= tag %></div>
            <% }); %>
        </div>

        <div class="row" id="resultsList">
            <% results.forEach(function(result){ %>
                <div class="col-12 col-sm-6 col-md-4 col-lg-3 col-xl-2 video-item" data-tags="<%= result.tags.join(',') %>">
                    <div class="video-card" onclick="location.href='/watch?id=<%= result.yid %>'">
                        <img src="https://img.youtube.com/vi/<%=result.yid %>/hqdefault.jpg" class="video-thumbnail" alt="Thumbnail">
                        <div class="video-info">
                            <div class="video-details w-100">
                                <div class="video-title text-white"><%= result.fileName %></div>
                                <div class="video-meta">
                                    <% if (result.uploader) { %>
                                        <div class="mb-1">
                                            <a href="/channel?name=<%= encodeURIComponent(result.uploader) %>" onclick="event.stopPropagation();" style="color: #aaaaaa; text-decoration: none; font-size: 0.85rem;">
                                                <%= result.uploader %>
                                            </a>
                                        </div>
                                    <% } %>
                                    <% result.tags.forEach(function(tag){ %>
                                        <span class="tag-badge"><%= tag %></span>
                                    <% }); %>
                                </div>
                                <div class="mt-2">
                                    <button class="btn btn-sm btn-outline-danger" onclick="event.stopPropagation(); if(confirm('Supprimer cette vidéo ?')) window.location.href='/delete?id=<%= result.yid %>'">
                                        Supprimer
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        function filterByTag(tag, element) {
            // Update active state
            document.querySelectorAll('.filter-chip').forEach(chip => chip.classList.remove('active'));
            element.classList.add('active');

            const items = document.querySelectorAll('.video-item');
            
            if (tag === 'all') {
                items.forEach(item => item.style.display = 'block');
                return;
            }

            items.forEach(item => {
                const itemTags = item.getAttribute('data-tags').split(',');
                if (itemTags.includes(tag)) {
                    item.style.display = 'block';
                } else {
                    item.style.display = 'none';
                }
            });
        }

        // Le téléchargement est géré par renderer.js via IPC.
        // On peut ajouter un fallback au cas où renderer.js ne se charge pas correctement.
        const commandForm = document.getElementById('commandForm');
        commandForm.addEventListener('submit', (e) => {
            // Si window.electronAPI n'existe pas, on utilise la route Express classique
            if (!window.electronAPI) {
                e.preventDefault();
                const url = document.getElementById('commandInput').value;
                window.location.href = `/download?url=${encodeURIComponent(url)}`;
            }
        });
    </script>

    <!-- Tawk.to Script -->
    <script type="text/javascript">
        var Tawk_API=Tawk_API||{}, Tawk_LoadStart=new Date();
        (function(){
            var s1=document.createElement("script"),s0=document.getElementsByTagName("script")[0];
            s1.async=true;
            s1.src='https://embed.tawk.to/673f4ae72480f5b4f5a1dbce/1id7jp68o';
            s1.charset='UTF-8';
            s1.setAttribute('crossorigin','*');
            s0.parentNode.insertBefore(s1,s0);
        })();
    </script>
</body>
</html>
