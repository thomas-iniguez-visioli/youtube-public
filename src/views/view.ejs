<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <style>
      body {
        max-width: 100%;
        height: 100vh;
        background-color: rgb(14, 14, 14);
        display: flex;
        flex-direction: column;
        margin: auto;
        align-items: center;
        justify-content: center;
        color: #ffffff; /* Change la couleur du texte à blanc */
      }
    </style>
  </head>
  <body>
    <video id="videoPlayer" width="70%" controls autoplay=false >
      <source src="/video/?id=<%= code %>" type="video/mp4" />
    </video>
    <a href="/" style="color: #ffffff;">Retour au menu principal</a>
    <a href="/delete?id=<%= code %>" style="color: #ffffff;">Supprimer</a>
    <p style="color: #ffffff;"><%= videodata.description%></p>
  <div style="margin-top: 20px;">
    <h2>Commentaires</h2>
    <% videodata.comments.forEach(comment => { %>
      <p style="color: #ffffff;"><%= comment %></p>
    <% }); %>
  </div>
  </body>
  <script>
  document.addEventListener('DOMContentLoaded', function() {
    document.getElementById('videoPlayer').muted = false;
    document.getElementById('videoPlayer').play();
  });
  </script>
</html>